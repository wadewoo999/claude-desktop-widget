{
  "name": "claude-widget",
  "version": "1.0.0",
  "description": "Claude Desktop usage monitor widget",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . \"Claude Widget\" --platform=win32 --arch=x64 --out=dist --overwrite --asar --ignore=dist --ignore=node_modules/electron$"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "@electron/packager": "^18.0.0"
  }
}
